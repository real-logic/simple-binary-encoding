name: "SBE CodeQL Scanning"

queries:
  - uses: security-and-quality

query-filters:
  - exclude:
      id: java/iterator-implements-iterable
  - exclude:
      id: java/missing-override-annotation
  - exclude:
      id: cs/useless-assignment-to-local
